<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.MyApplication" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.MyApplication" parent="Base.Theme.MyApplication" >
        <item name="android:fontFamily">@font/palanquin</item>
    </style>

    <style name="EditText_Style">
        <item name="android:paddingStart">24dp</item>
        <item name="android:background">@drawable/edittext_background</item>
        <item name="android:minHeight">52dp</item>
        <item name="android:padding">10dp</item>
        <item name="android:layout_marginRight">@dimen/activity_horizontal_margin</item>
        <item name="android:layout_marginLeft">@dimen/activity_horizontal_margin</item>
        <item name="android:layout_marginTop">@dimen/activity_horizontal_margin</item>
        <item name="color">@color/black</item>
    </style>

    <style name="Button64_Style" parent="ShapeAppearanceOverlay.Material3.Button">
        <item name="backgroundTint">@color/orange</item>
        <item name="shapeAppearanceOverlay">@style/Button_Rounded</item>
        <item name="android:minHeight">56dp</item>
        <item name="android:minWidth">104dp</item>
        <item name="android:padding">4dp</item>
        <item name="color">@color/white</item>
    </style>

    <style name="Button_Rounded">
        <item name="cornerFamily">rounded</item>
        <item name="cornerSize">16dp</item>
    </style>

    <style name="Image_Rounded">
        <item name="cornerFamily">rounded</item>
        <item name="cornerSize">50%</item>

    </style>

</resources>